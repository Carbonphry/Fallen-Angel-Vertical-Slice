depth = -y

if (last_sprite != sprite_index) 
{
   image_index = 0;
   last_sprite = sprite_index;
}

//mp_potential_step_object(class_player.x, class_player.y, .7, class_enemy_coll);

//point_direction(x,y,o_player.x, o_player.y);
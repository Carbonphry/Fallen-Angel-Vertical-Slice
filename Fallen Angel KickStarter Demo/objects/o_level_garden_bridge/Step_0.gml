/// @description Insert description here
// You can write your code in this editor
/*if o_player.current_layer_ == LAYER1  {
	depth = o_player.depth-1; 
} else if o_player.current_layer_ == LAYER2 or o_player.current_layer_ == LAYER3 {
	depth = o_player.depth+1; 
}*/
/*
if o_player.z >= 32 {
	depth = o_player.depth +1;
	instance_activate_object(block);
} else {
	depth = o_player.depth -1;
	instance_deactivate_object(block);
}